---
name: cat-mode
description: >
  Use when the user says cat-mode or /cat-mode, or wants this working style
  (preferences and session overrides). Not a delivery stage. Do not start
  keel route, design, dev, or end-to-end.
disable-model-invocation: true
---

# cat-mode

风格 skill，不是交付环节，不是流程。

停手以 `../keel/references/when-to-ask.md` 为准（先完整读取），不复制该文件中的表。
写设计以 `../keel/references/when-to-write.md` 为准（先完整读取），不复制该文件中的表。

会话覆盖：「别停」只加强 `when-to-ask.md` 里默认可做的项，不能授权该文件里要问人的项。

用户说处理 Issue、端到端或 `/keel` → 交给 `keel`。本 skill 不启动 route / design / dev / end-to-end。
